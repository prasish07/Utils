<% contentData.forEach(function(content) { %>
    <div class="content">
        <h1 class="content__title">
            <%= content.title %>
        </h1>
        <p class="content__paragraph">
            <%- content.paragraph %>
        </p>
    </div>
    <% }); %>